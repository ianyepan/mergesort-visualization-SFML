#pragma once

namespace constants {

inline constexpr unsigned int windowWidth = 800;
inline constexpr unsigned int windowHeight = 400;
inline constexpr int numCount = 200;
inline constexpr float heightScale = 0.9;
inline constexpr unsigned int delay = 8000;

}  // namespace constants

namespace global {

float barWidth;

}
